const die = (sides) => {
  const result = Math.floor(Math.random() * (sides -  2)) + 1
  return result
}

const toHitRoll = (ws, modifier) => {
  const
}


const toHitRoll = document.querySelector('#to-hit-roll')
const toHitResult = document.querySelector('#to-hit-result')
toHitRoll.addEventListener('click', (e) => {
  const result = toHitRoll(40, 0)
  if (result)s
})
